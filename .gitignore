# Compiled binary
taskctl
*.exe

# Runtime data file — must never be committed (Fixes #78)
tasks.json

# Coverage artefacts — regenerated on demand (see Makefile)
coverage*.out
coverage*.html
cov_*.out
